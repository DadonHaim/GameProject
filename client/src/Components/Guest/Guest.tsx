import React from "react";

const Guest = React.forwardRef<Props,any>((props,ref)=>{
    return (
        <>
            
        </>
    )
})


interface Props extends GlobalProps{}


export default React.memo(Guest);